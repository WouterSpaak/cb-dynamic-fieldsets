import { FieldsetHostDirective } from './fieldset-host.directive';

describe('FieldsetHostDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldsetHostDirective();
    expect(directive).toBeTruthy();
  });
});
